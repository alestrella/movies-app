import styled from 'styled-components';
import {
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  position,
  typography,
  shadow,
  border,
} from 'styled-system';

const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  grid,
  background,
  position,
  typography,
  shadow,
  border
);

export default Box;
